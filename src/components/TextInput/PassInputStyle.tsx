import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  inputer: {
    width: '100%',
    backgroundColor: '#d0d0d0',
    color: '#333',
    fontSize: 20,
    height: 48,
    padding: 10,
    textAlign: 'center',
    borderWidth: 2,
    borderRadius: 5,
    borderColor: '#333',
  }
})
